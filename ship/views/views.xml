<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="ship.list">
      <field name="name">Ship list</field>
      <field name="model">ship.ship</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="ship.action_window">
      <field name="name">Ship window</field>
      <field name="res_model">ship.ship</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="ship.action_server">
      <field name="name">Ship server</field>
      <field name="model_id" ref="model_ship_ship"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="Ship" id="ship.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="ship.menu_1" parent="ship.menu_root"/>
    <menuitem name="Menu 2" id="ship.menu_2" parent="ship.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="ship.menu_1_list" parent="ship.menu_1"
              action="ship.action_window"/>
    <menuitem name="Server to list" id="ship" parent="ship.menu_2"
              action="ship.action_server"/>
-->
  </data>
</odoo>