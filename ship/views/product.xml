<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="product_act" model="ir.actions.act_window">
            <field name="name">Sản Phẩm</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">product.template</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Tạo sản phẩm ở đây
              </p>
                <tree>
            <field name="Color"/>
            <field name="type"/>
            <field name="Capacity"/>
            <field name="amount"/>
            <field name="Insurance"/>
                    <field name="description"/>
                </tree>
            </field>

        </record>

         <menuitem id="ship_menu"
              name="Ship"
              sequence="10"/>
    <menuitem id="product_menu_item"
              name="product"
              parent="ship_menu"
              sequence="10"/>
    <menuitem id="product_menu"
              name="product"
              parent="product_menu_item"
              action="product_act"
              sequence="10"/>

    </data>
</odoo>